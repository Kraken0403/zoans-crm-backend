<!DOCTYPE html>
<html>
  <head>
    <style>
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333;
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
        border: 1px solid #ddd;
        border-radius: 5px;
        background-color: #f9f9f9;
      }
      .header {
        text-align: center;
        font-size: 20px;
        margin-bottom: 20px;
        color: #555;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h2>Lead Assigned to you</h2>
      </div>
      <p>Lead Name: {{ first_name }} {{ last_name }},</p>
      <p>Email: {{ email }}</p>
      <p>Phone Number: {{ phone_number }}</p>
      <p>Organisation Name: {{ company_name }}</p>
      <p>Lead Status: {{ lead_status }}</p>
      <p>priority: {{ priority }}</p>
      <p>Fields: {{ custom_fields }}</p>
      <p>Amount: {{ amount }}</p>
    </div>
  </body>
</html>
